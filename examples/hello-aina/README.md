# 问候生成器 AINA

这是一个最小的 managed AINA 示例，根据姓名生成中文或英文问候语。

示例输入：

```json
{"name": "小明", "language": "zh"}
```

示例输出：

```text
你好，小明！欢迎使用 Unibot。
```

导入项目 ZIP 后，在能力中心完成“部署”和“安装并授权”，Unibot 即可从对话中路由并调用该入口。
